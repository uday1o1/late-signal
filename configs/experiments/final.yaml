version: 1
mode: final
protocol: ../protocol.yaml
methods:
  - complete_wait
  - immediate_fake_negative
  - fixed_wait
  - dfm
  - fnw
  - es_dfm
  - oracle_reference
schedulers:
  - fixed_early
  - fixed_midpoint
  - fixed_deadline
  - calibration_drift
offline_references:
  - mature_logistic_regression
  - mature_lightgbm
target_device: cuda
require_real_pilot: true
pilot:
  prepared_root: ../../data/processed
  max_click_days: 2
  benchmark_examples: 4096
  benchmark_batch_size: 128
  benchmark_steps: 5
  assumed_source_archive_gb: 2.0
  assumed_expanded_source_gb: 6.0
  assumed_prepared_data_gb: 12.0
  temporary_margin_gb: 5.0
  assumed_host_memory_gb: 16.0
caps:
  max_runs: null
  max_gpu_hours: null
  max_working_disk_gb: null
  max_retained_disk_gb: null
