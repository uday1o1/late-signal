version: 1
mode: final
protocol: ../protocol.yaml
methods:
  - complete_wait
  - immediate_fake_negative
  - fixed_wait
  - dfm
  - fnw
  - es_dfm
  - oracle_reference
schedulers:
  - fixed_early
  - fixed_midpoint
  - fixed_deadline
  - calibration_drift
offline_references:
  - mature_logistic_regression
  - mature_lightgbm
target_device: cuda
require_real_pilot: true
pilot:
  prepared_root: ../../data/processed
  execution_host_has_source_artifacts: false
  max_click_days: 2
  benchmark_examples: 4096
  benchmark_batch_size: 2048
  benchmark_steps: 5
  assumed_source_archive_gb: 2.0
  assumed_expanded_source_gb: 6.0
  assumed_prepared_data_gb: 2.5
  assumed_runtime_feature_cache_gb: 2.0
  temporary_margin_gb: 5.0
  assumed_host_memory_gb: 16.0
caps:
  max_runs: 89
  max_gpu_hours: 4.0
  max_working_disk_gb: 25.0
  max_retained_disk_gb: 2.0
